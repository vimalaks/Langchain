services:
  - type: web
    name: groq-langchain-service
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    envVars:
      - key: GROQ_API_KEY
        sync: false  # You'll add the value manually in the dashboard